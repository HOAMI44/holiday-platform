spring:
  application:
    name: booklet-service

server:
  port: 8087

jwt:
  secret: ${JWT_SECRET:holidayplanner-local-test-secret}

service:
  secret: ${SERVICE_SECRET:holidayplanner-local-service-secret}

services:
  organization-service:
    url: ${SERVICES_ORGANIZATION_SERVICE_URL:http://localhost:8084}
  event-service:
    url: ${SERVICES_EVENT_SERVICE_URL:http://localhost:8081}
